before_install:
 - chmod +x gradlew
 
language: android

android:
  components:
    - tools
    - build-tools-23.0.2
    - android-23
    - extra-android-m2repository

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    
script: ./gradlew build check

deploy:
  provider: bintray
  file: "bintray.json"
  user: "natinusala"
  key: "9b982b2b9a0873db105702cb6f8f394636f45ac5"
